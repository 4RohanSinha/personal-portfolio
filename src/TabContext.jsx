import { createContext } from "react";

export const TabContext = createContext({});
export const ProjectsContext = createContext([]);
export const BlogsContext = createContext([]);
export const BlogViewContext = createContext([]);